class BusLocationDetector{

    constructor(umbral) {
        this.umbral = umbral;
    }

    detectLocations() {
        detectedBUs = new Array();
        return detectedBUs;
    }
}

module.exports = BusLocationDetector;