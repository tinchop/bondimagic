class BusLocationDetector{

    constructor(umbral) {
        this.umbral = umbral;
    }

    detectLocations() {
        detectedBus = [];
        return detectedBus;
    }
}

module.exports = BusLocationDetector;